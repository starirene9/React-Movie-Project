let iframes = document.querySelector('iframe');

let videoSrc = [
    '',
    '',
    ''
]

// iframes.src = 'https://www.youtube.com/embed/' + videoSrc[__index] + '?autoplay=1&mute=1';

export default videoSrc